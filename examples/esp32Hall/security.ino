/**
 * Função de middleware, que recebe todos as requisições que são solicitados 
 * e analisa se o acesso está liberado ou não.
 */
bool accessManagement(char *bufferResposta, Resource *rsc, char *data) {
  return true;
}
